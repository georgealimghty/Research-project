---
# Simple test configuration for the matching segment
# This demonstrates flow processing without external dependencies

- segment: stdin
  config:
    filename: "sample_flows.json"

- segment: matching
  config:
    # Automatically loads from nerd_export.csv

- segment: printflowdump
  config:
    verbose: true 